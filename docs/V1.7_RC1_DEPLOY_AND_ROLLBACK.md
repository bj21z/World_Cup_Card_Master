# V1.7 RC1 部署与回滚

## 部署
必须全量替换Alpha 4文件，不建议只上传差异文件。部署后强制刷新浏览器缓存。

## 升级前
在游戏内导出存档备份；保留Alpha 4完整压缩包。

## 回滚
1. 用Alpha 4完整文件覆盖RC1。
2. 强制刷新缓存。
3. 若RC1期间存档出现异常，导入升级前备份。

RC1与Alpha 4均为Schema 23，但回滚前仍建议保留备份。
