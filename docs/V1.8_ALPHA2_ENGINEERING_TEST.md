# V1.8 Alpha 2 工程测试报告

测试日期：2026-08-07
测试性质：本地工程静态/核心逻辑测试，不替代 iPhone Safari 真机验收。

## 已实际执行
- 全部 `data/*.js` 与 `js/*.js` 使用 Node.js `--check` 语法扫描：PASS。
- `index.html` 本地 `src/href` 引用扫描：48 个引用，缺失 0。
- `showMatchEvent` 定义检查：1 个，已消除 V1.7 包中重复定义造成的覆盖风险。
- `afterTourGroupMatch` 定义检查：1 个。
- V1.8 核心逻辑单元冒烟：Event Ledger 成功提交 1 个 `SHOT_SAVED` 事件；生成 5 条 Effect；动态时间 `67:24`；Integrity Checker = PASS。
- V1.7.0 母版 SHA-256 基线清单已生成。

## 本轮没有宣称完成
- iPhone Safari 真实设备连续比赛。
- 世界杯完整小组赛→淘汰赛→夺冠/淘汰的 V1.8 真机回归。
- 1000 场完整浏览器端自动模拟。
- 弱网、锁屏、后台恢复的真实移动端验证。

这些项目保留为 V1.8 Alpha 2 后续真机门禁，不能用本报告代替。
