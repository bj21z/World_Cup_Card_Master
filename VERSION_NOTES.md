# 版本说明：World_Cup_Card_Master_V1.1

## 本次改动摘要
V1.1 把 V1.0 的“十一人翻牌对决”升级为“战术三牌版”：每回合可能出现 1–3 张牌的局部攻防，并引入组合技能、阵型、士气、体能、进球概率、小组赛、最好第三名、淘汰赛和点球大战。

## 新增文件
- `css/style.css`：独立样式文件，优化手机端布局和视觉层级。
- `data/players.js`：球员卡数据，继承 V1.0 精细球员卡。
- `data/teams.js`：世界杯分组、球队强度、阵型和风格。
- `data/balance.js`：比赛场景、能力权重、进球概率和奖励参数。
- `data/skills.js`：球星技能关键词与加成规则。
- `data/combos.js`：组合技能、国家队连携和通用战术连携。
- `js/storage.js`：本地存档读写。
- `js/growthEngine.js`：球员标准化、模拟卡、训练、经验和成长。
- `js/matchEngine.js`：战术三牌比赛核心引擎。
- `js/tournamentEngine.js`：世界杯征途、小组积分、晋级和淘汰赛。
- `js/render.js`：卡片、表格、雷达图和弹窗渲染。
- `js/app.js`：主交互逻辑。

## 修改文件
- `index.html`：从单文件结构改成模块化入口；新增大厅、比赛中心、世界杯征途、球员卡册、训练中心、规则说明六个板块。
- `README.md`：更新运行说明、玩法介绍和数据口径。
- `VERSION_NOTES.md`：新增本版本详细说明。

## 删除文件
- 无。

## 保持不变内容
- 保留 V1.0 的核心球员卡、雷达图维度、金币、经验、训练强化、本地存档理念。
- 保留手机端直接打开运行的轻量化方式。

## 是否需要全量替换
建议全量替换。V1.1 文件结构已从单一 HTML 改为多文件工程，不建议只替换 `index.html`。

## 部署步骤
1. 解压 `World_Cup_Card_Master_V1.1.zip`。
2. 将整个 `World_Cup_Card_Master_V1.1` 文件夹上传到 GitHub 仓库或 Cloudflare Pages。
3. 保持目录结构不变，尤其是 `css/`、`js/`、`data/` 文件夹。
4. 部署后打开首页即可运行。

## 缓存刷新
- iPhone Safari：长按刷新或关闭页面重新打开。
- Cloudflare Pages：重新部署后可等待 1–3 分钟；必要时更改文件名或清理缓存。
- 如果本地存档残留影响测试，可在“训练中心”点击“清除全部存档”。

## 回滚说明
如 V1.1 上传后出现问题，可直接恢复 V1.0 压缩包中的 `index.html` 单文件版本；或在 GitHub 中回退到上一提交。

## 测试结果
- 已检查 JS 语法。
- 已检查 HTML/CSS/JS 文件齐全。
- 已打包为 ZIP。
- 已确认核心函数包括快速比赛、翻牌回合、自动完成、世界杯征途、球员卡册、训练中心、存档读写。

## 已知限制
- V1.1 不是完整 1248 名球员数据终版。
- 暂无球员照片、音效、PWA 安装包、联网更新和在线对战。
- 未精细录入球队使用模拟卡，后续建议逐步补齐真实球员卡数据库。
