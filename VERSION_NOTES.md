# V1.0 发布说明

## 本次改动摘要
首次完整发布，建立球员数据、卡牌展示、雷达图、比赛引擎、成长系统和本地存档。

## 文件清单
- `index.html`：完整游戏程序，包含页面、样式、球员数据库与比赛逻辑。
- `README.md`：运行方法、功能说明、数据口径和限制。
- `VERSION_NOTES.md`：版本变动和部署说明。

## 新增文件
以上 3 个文件均为首次新增。

## 删除文件
无。

## 是否需要全量替换
是。V1.0 为首次发布，应完整上传整个文件夹。

## 部署步骤
1. 将三个文件放在同一目录。
2. GitHub 仓库根目录必须能看到 `index.html`。
3. Cloudflare Pages 构建命令留空，输出目录填写 `/` 或保持默认。
4. 部署后如仍显示旧版，清理浏览器缓存或使用无痕窗口打开。

## 回滚说明
保留旧文件夹即可回滚；本版浏览器存档键为 `wc-card-master-v1`，不会影响其他项目。

## 测试结果
- 页面离线打开：通过。
- 8 队切换：通过。
- 88 人卡牌渲染：通过。
- 雷达图：通过。
- 11 回合比赛与结算：通过。
- 金币、经验、强化、本地存档：通过。
- iPhone 窄屏布局：已适配。
