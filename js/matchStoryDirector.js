(function(){function classify(ms){let ev=ms?.eventLedger||[],goals=ev.filter(e=>e.type==='GOAL').length,cards=ev.filter(e=>/CARD/.test(e.type||'')).length,d=Math.abs(Number(ms?.scoreA||0)-Number(ms?.scoreB||0));if(cards>=2)return'纪律改变比赛';if(goals>=4)return'对攻大战';if(Number(ms?.minute||0)>=75&&d===0)return'末段胶着';if(d>=3)return'一方压制';return'战术拉锯'}window.MatchStoryDirector={classify};})();